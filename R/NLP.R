#' Tokenize, process, and visualise text data using Natural Language Processing
#'
#' Simple workflows build on the tidytext package. Multiple dictionaries/lexicons are provided: \code{\link{emotions}}, \code{\link{valence}}, and \code{\link{relations}}. Plotting is done via ggplot2.
#'
#' Use \code{\link{tidy_parcels}} to tokenize data, then \code{\link{categorize_parcels}} to process it using a dictionary. Finally, plot it with \code{\link{plot_percentages}} before analysis.
"_PACKAGE"
#> [1] "_PACKAGE"
